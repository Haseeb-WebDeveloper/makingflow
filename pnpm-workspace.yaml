allowBuilds:
  # esbuild's postinstall just downloads its platform binary — required by
  # drizzle-kit and vitest, safe to allow.
  esbuild: true
  sharp: false
  unrs-resolver: false
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
