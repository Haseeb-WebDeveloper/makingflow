import { AccountSkeleton } from "@/components/dashboard/skeletons/settings-skeletons"

/** See settings-skeletons.tsx: the form chrome is real, the values wait. */
export default function Loading() {
  return <AccountSkeleton />
}
