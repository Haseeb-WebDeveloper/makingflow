// Drizzle schema — single source of truth for the database.
// Define tables with pgTable() here, then `pnpm db:generate` + `pnpm db:migrate`.
//
// Empty for now; the MakingFlow schema (workspaces, forms, fields,
// submissions, members, billing) lands here as the product is built.

export {}
